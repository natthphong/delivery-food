# ----- Vercel Postgres -----
POSTGRES_URL=REPLACE_ME
POSTGRES_URL_NON_POOLING=REPLACE_ME
POSTGRES_PRISMA_URL=REPLACE_ME
POSTGRES_USER=REPLACE_ME
POSTGRES_HOST=REPLACE_ME
POSTGRES_PASSWORD=REPLACE_ME
POSTGRES_DATABASE=REPLACE_ME

# Legacy fallbacks (only needed for existing local setups)
NEXT_PUBLIC_DELIVERY_POSTGRES_URL=
NEXT_PUBLIC_DELIVERY_POSTGRES_URL_NON_POOLING=
NEXT_PUBLIC_DELIVERY_POSTGRES_PRISMA_URL=
NEXT_PUBLIC_DELIVERY_POSTGRES_USER=
NEXT_PUBLIC_DELIVERY_POSTGRES_HOST=
NEXT_PUBLIC_DELIVERY_POSTGRES_PASSWORD=
NEXT_PUBLIC_DELIVERY_POSTGRES_DATABASE=

# ----- S3 (unused here but kept) -----
NEXT_PUBLIC_S3_ACCESSKEY=REPLACE_ME
NEXT_PUBLIC_S3_SECRET_KEY=REPLACE_ME
NEXT_PUBLIC_S3_NAME=REPLACE_ME

# ----- Firebase (client) -----
NEXT_PUBLIC_FIREBASE_API_KEY=REPLACE_ME
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=REPLACE_ME         # e.g. your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=REPLACE_ME

# ----- Firebase (server/admin) -----
# Put the full JSON of your service account, base64-encoded:
#   cat serviceAccount.json | base64 | pbcopy
FIREBASE_SERVICE_ACCOUNT_B64=REPLACE_ME

# ----- JWT (server) -----
JWT_SECRET=replace_me_with_a_long_random_string
JWT_EXPIRES_IN=900            # seconds (15m)
REFRESH_TOKEN_EXPIRES_IN=604800   # seconds (7d)
